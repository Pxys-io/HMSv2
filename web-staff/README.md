# web-staff — HMSv2 staff workspace

Internal app: secretary / doctor / admin + doctor PWA (Phase 10).

- Dev: `npm run dev` (port 5173, proxies `/api` → `http://localhost:8000`)
- Lint: `npm run lint` · Test: `npm test` · Build: `npm run build`
- Design tokens: `src/styles/tokens.css` (Plan/00 — the only place hex lives)
